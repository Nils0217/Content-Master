# Core package dependencies (mirrors pyproject.toml's [project.dependencies]
# — pinned here for `pip install -r requirements.txt` without the package
# itself). Prefer `pip install -e .` instead; it installs these
# automatically and also registers the `contentmaster` CLI command.
requests==2.34.2
python-dotenv==1.2.3
atproto==0.0.72
striprtf==0.0.33
duckdb==1.5.5

# warehouse/ (dbt + DuckDB analytics) is a separate concern with its own
# large dependency tree — install it only if you're running dbt yourself:
#   pip install dbt-duckdb
# (or `pip install -e ".[warehouse]"`). See warehouse/README.md.
