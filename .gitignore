.env
.venv/
__pycache__/
*.pyc
audit/*.jsonl
hydradb-data/store/
hydradb-data/cache/
plays/*.json
plays/topics/*.json
plays/_failures.jsonl
plays/_history.jsonl
plays/_tracking_review.jsonl
plays/_draft_review.jsonl
plays/_analysis_review.jsonl
plays/_products.jsonl
plays/_characteristics.jsonl
plays/_test_axes.jsonl
metrics/*.jsonl
.rocketride/
pipelines/services-catalog-reference.json
.DS_Store
Demo-white paper/
Dirty_data_raw/
generated_images/
warehouse/local.duckdb
warehouse/*.duckdb.wal
warehouse/target/
warehouse/logs/
warehouse/dbt_packages/
warehouse/.user.yml
*.egg-info/
build/
dist/

# Isolated data root for test runs (CONTENTMASTER_DATA_ROOT, see env.example)
.testdata/

# era 0 archive — the old test account's data, kept locally only.
# NOTE: these files exist on this machine and nowhere else. They are the
# only record of how a completely unmanaged account performed (see
# docs/ERA0_SNAPSHOT.md), and git is not backing them up.
archive/
